import { Box } from "@mui/material";
import styled from "styled-components";

export const ChipButtonStyle = styled(Box)`
  .MuiChip-root {
    span {
      padding: 10px;
      padding-bottom: 13px;
    }
  }
`;
